const order = ["chicken", "Onion", "Tomoto"];
const order2 = ["Cheese", "Lettuce", "Tomato"];
const order3 = ["Ketchup", "Mustard", "Mayo"];

function foodOrder(items) {
  console.log(`items for sandwich: (${items} )`);
}

foodOrder(order);
foodOrder(order2);
foodOrder(order3);
