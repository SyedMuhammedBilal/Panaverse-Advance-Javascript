function make_shirt(size = "large", text = "I love JavaScript") {
  if (size.length < 0 || size.length < 0) {
    console.log(
      `this size of the Tshirt is ${size} and the message printed on it is ${text}`
    );
  } 

  console.log(
    `this size of the Tshirt is ${size} and the message printed on it is ${text}`
  );
}
make_shirt();
