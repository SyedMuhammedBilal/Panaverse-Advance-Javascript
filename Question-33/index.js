const favPizza = ["Veggie Pizza", "Pepperoni Pizza", "afghai tikka Pizza"];

for (let index = 0; index < favPizza.length; index++) {
  const element = favPizza[index];
  console.log(`I like ${element} `);
}
